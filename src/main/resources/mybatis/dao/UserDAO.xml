<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="dev.zacharyajohnson.wanikani.desktop.dao.UserDAO">
    <select id="getUser" parameterType="String" resultType="User">
        SELECT
               *
        FROM
             USER
        WHERE
            ID = #{id}
    </select>

    <insert id="createUser" parameterType="User">
        INSERT INTO
            USER(ID, USER_NAME, LEVEL, API_KEY, LOGGED_IN)
            VALUES(#{id}, #{username}, #{level}, #{apiKey}, #{isLoggedIn})
    </insert>

    <select id="getLoggedInUser" resultType="User">
        SELECT *
        FROM
            USER
        WHERE
            LOGGED_IN = true
    </select>

    <update id="updateUser" parameterType="User">
        UPDATE USER
            SET LOGGED_IN = #{isLoggedIn}, USER_NAME = #{username}, LEVEL = #{level},
                API_KEY = #{apiKey}
            WHERE ID = #{id}
    </update>
</mapper>
